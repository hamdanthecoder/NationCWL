[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install Flask==3.0.0 Flask-SQLAlchemy==3.1.1 Werkzeug==3.0.1 gunicorn==21.2.0 PyMySQL==1.1.0 cryptography==41.0.7"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
